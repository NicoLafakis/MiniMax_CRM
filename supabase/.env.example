# ---------------------------------------------
# Supabase Edge Functions environment example
# Copy to supabase/.env for local functions serve
# In hosted Supabase, set these with: supabase secrets set --env-file supabase/.env
# ---------------------------------------------

# Supabase project settings (required by Edge Functions)
SUPABASE_URL=https://YOUR-PROJECT-ID.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY

# Optional CORS origins; functions currently default to "*" in code
# Set to your frontend URL(s) in production, e.g. https://crm.example.com,https://staging.example.com
ALLOWED_ORIGINS=*

# Note: OpenAI API keys are stored per-user in the user_settings table and
# fetched by Edge Functions at runtime. No global OPENAI_API_KEY is required.
