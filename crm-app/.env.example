# ---------------------------------------------
# Frontend (Vite) environment example
# Copy to .env.local (for local dev) or .env
# Only variables prefixed with VITE_ are exposed to the browser
# ---------------------------------------------

# Supabase project settings (required)
VITE_SUPABASE_URL=https://YOUR-PROJECT-ID.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Build-time toggle used in vite.config.ts to disable dev-only plugins
# NOTE: This is read by Node during the Vite config phase (process.env.BUILD_MODE).
# It is NOT exposed to the client. Prefer setting from your shell for builds.
# Windows PowerShell example:
#   $env:BUILD_MODE = "prod"; pnpm build:prod
# For development, you can keep this unset or set to "dev"
BUILD_MODE=dev
